# ============================================================================
# CRANE-X7 VLA Training Environment Configuration Template
# ============================================================================
# このファイルを .env にコピーして、環境に合わせて値をカスタマイズしてください
#
# 使い方:
#   cp .env.template .env
#   # .env を編集して設定を変更
#   docker compose --profile <プロファイル名> up
# ============================================================================

# ----------------------------------------------------------------------------
# API Keys & Authentication
# ----------------------------------------------------------------------------
# Weights & Biases API key (実験トラッキング用)
# https://wandb.ai/settings で取得
WANDB_API_KEY=

# W&Bモード (disabled: ロギング無効, online: オンライン, offline: オフライン)
WANDB_MODE=disabled

# HuggingFace API token (事前学習済みモデルのダウンロード用)
# https://huggingface.co/settings/tokens で取得
HF_TOKEN=

# ----------------------------------------------------------------------------
# GPU Configuration
# ----------------------------------------------------------------------------
# 使用するGPU ID (カンマ区切りで複数指定可能、allですべてのGPUを使用)
# 例: 0, 1, 0,1,2,3, all
CUDA_VISIBLE_DEVICES=0

# 使用するGPU数 (allですべてのGPUを使用)
GPU_COUNT=all

# 共有メモリサイズ (大規模モデルのトレーニング用)
# デフォルト: 16gb
SHM_SIZE=16gb

# ----------------------------------------------------------------------------
# TensorFlow Configuration
# ----------------------------------------------------------------------------
# TensorFlowログレベル (0=全て, 1=INFO以下を抑制, 2=WARNING以下を抑制, 3=ERROR以下を抑制)
TF_CPP_MIN_LOG_LEVEL=2
